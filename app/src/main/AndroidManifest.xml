<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mockpropertymanagmentapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.todolist.activities.AddTaskActivity" />
        <activity android:name=".ui.otheractivities.VendorsActivity" />
        <activity android:name=".ui.otheractivities.AlertsActivity" />
        <activity android:name=".ui.properties.activities.AddNewPropertyActivity" />
        <activity android:name=".ui.otheractivities.DocumentActivity" />
        <activity android:name=".ui.otheractivities.TripActivity" />
        <activity android:name=".ui.todolist.activities.ToDoListActivity" />
        <activity android:name=".ui.otheractivities.CollectRentActivity" />
        <activity android:name=".ui.otheractivities.TransactionsActivity" />
        <activity android:name=".ui.otheractivities.TenantsActivity" />
        <activity android:name=".ui.properties.activities.PropertyActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.auth.activities.RegisterActivity" />
        <activity android:name=".ui.auth.activities.LoginActivity" />
        <activity android:name=".ui.otheractivities.StartActivity" />
        <activity android:name=".ui.otheractivities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>